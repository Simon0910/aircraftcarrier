package ${cfg.GatewayImpl_p};

import ${cfg.Gateway_f};
import ${cfg.Repository_f};
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

import javax.annotation.Resource;

/**
 * $!{table.comment} GatewayImpl
 *
 * @author ${author}
 * @date ${date}
 * @since 1.0
 */
@Slf4j
@Component
public class ${cfg.GatewayImpl_n} implements ${cfg.Gateway_n} {

    /**
     * ${cfg.Repository_n}
     */
    @Resource
    ${cfg.Repository_n} ${cfg.Repository_n_i};


}
